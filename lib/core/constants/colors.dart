import 'package:flutter/material.dart';

class TurfArdorColors {
  // BRAND GREENS (updated — deeper forest)
  static const Color accent = Color(0xFF4CB572);        // spring green — keep as-is
  static const Color accentDark = Color(0xFF004B3A);     // UPDATE from 0xFF135E4B to 0xFF004B3A (deeper forest)
  static const Color mint = Color(0xFFA4E4C1);           // NEW — mint wash for tags, tints, hover states
  static const Color emeraldForest = Color(0xFF135E4B);
  static const Color emeraldSpring = Color(0xFF4CB572);

  // LOVE + ROMANCE (updated + new)
  static const Color pink = Color(0xFFE8437A);           // keep as-is — hot pink for like button
  static const Color pinkBlush = Color(0xFFF8BBD0);      // NEW — blush pink for match cards, chat accents
  static const Color roseWhisper = Color(0xFFFFF0F5);    // NEW — rose tint for match overlay background
  static const Color passionRed = Color(0xFFC62828);     // NEW — super like, notification dots

  // CELEBRATION
  static const Color gold = Color(0xFFF2C233);           // keep as-is — premium gold for verifications
  static const Color champagneGlow = Color(0xFFFFF8E1);  // NEW — light festive yellow for cards, countdowns

  // Surface Colors - Light Mode
  static const Color backgroundLight = Color(0xFFF5F0E8);
  static const Color lightBackground = Color(0xFFF5F0E8);
  static const Color white = Colors.white;
  static const Color lightCard = Colors.white;
  static const Color lightBorder = Color(0xFFE8DDD0);

  // Surface Colors - Dark Mode
  static const Color backgroundDark = Color(0xFF080F0C);
  static const Color darkBackground = Color(0xFF080F0C);
  static const Color deepBackground = Color(0xFF080F0C);
  static const Color darkCard = Color(0xFF0D1A13);
  static const Color cardSurface = Color(0xFF0D1A13);
  static const Color darkElevated = Color(0xFF152B1E);
  static const Color inputSurface = Color(0xFF152B1E);

  // Text Colors
  static const Color textPrimaryLight = Color(0xFF0D2B1E);
  static const Color textPrimaryDark = Colors.white;
  static const Color textPrimary = Colors.white;
  static const Color mutedTextLight = Color(0xFF9BB3AF);
  static const Color textMuted = Color(0xFF9BB3AF);

  // Semantic
  static const Color error = Color(0xFFE83535);
  static const Color black = Colors.black;
}
